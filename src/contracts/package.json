{
  "name": "hsmcai-contracts",
  "version": "1.0.0",
  "description": "HSMC — Smart contracts",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "clean": "npx hardhat clean"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.19"
  }
}
